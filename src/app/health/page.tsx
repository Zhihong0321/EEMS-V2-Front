import HealthCheckConsole from "@/components/health/health-check-console";

export const metadata = {
  title: "Health Check | Eternalgy EMS"
};

export default function HealthPage() {
  return <HealthCheckConsole />;
}
