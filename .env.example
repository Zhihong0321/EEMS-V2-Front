NEXT_PUBLIC_BACKEND_URL=https://eternalgy-ems-backend.up.railway.app
NEXT_PUBLIC_BACKEND_TOKEN=dev-only-token
NEXT_PUBLIC_TIMEZONE_LABEL=Asia/Kuala_Lumpur

# Backend configuration used by Next.js proxy at /api/bridge
# These are read on the server (Node runtime) and can also be exposed on the client.
# Prefer keeping values in Railway service variables.
NEXT_PUBLIC_BACKEND_URL=https://eems-v2-backend-production.up.railway.app
NEXT_PUBLIC_BACKEND_TOKEN=01121000099
